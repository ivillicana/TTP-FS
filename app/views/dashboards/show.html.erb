<div class="container">
  <div class="wrapper">
    <div class="header">
      <h1>Portfolio</h1>
      <div class="name"><%= @user.name %></div>
      <div class="email"><%= @user.email %></div>
      <div class="value"><%= number_to_currency(@portfolio[:total_value]) %></div>
    </div>

      <div id="portfolio-stocks">
        <% @portfolio[:companies].each do |company_name, comp_hash| %>
          <div id="<%= company_name %>" class="transaction-item">
            <div class="portfolio-stock-name">
              <%= company_name %>
            </div>
            <div class="portfolio-item-shares">
              <%= pluralize(comp_hash[:shares], 'share')%>
            </div>
            <div class="portfolio-stock-value">
              <%= number_to_currency(comp_hash[:value]) %>
            </div>
          </div>
        <% end %>
      </div>
      
      <div id="portfolio-stock-buy">
        <div class="stock-buy-header">
          <h2>Buy Stock</h2>
          <p>Cash - $<%= @user.balance %></p>
        </div>
        <div class="purchase-form">
          <%= form_tag('/transactions') do %>
            <div><%= text_field_tag 'ticker_symbol', nil, placeholder: "Stock Symbol" %></div>
            <div><%= text_field_tag 'shares_quantity', nil, placeholder: "Quantity" %></div>
            <div><%= submit_tag 'Buy' %></div>
          <% end %>
        </div>
      </div>
  </div>
</div>